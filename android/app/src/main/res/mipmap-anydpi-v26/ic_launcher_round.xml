<?xml version="1.0" encoding="utf-8"?>
<!-- Built from the channel poster by scripts/make_icons.py.
     The title text is deliberately not in the icon: at 48dp
     "TECHNICAL JAADUGAR" is an unreadable smear, so the icon carries the
     magician on the poster's curtain red and the name comes from the label
     underneath it. -->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ic_launcher_background" />
    <foreground android:drawable="@mipmap/ic_launcher_foreground" />
</adaptive-icon>
