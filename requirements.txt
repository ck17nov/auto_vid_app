# ---- AutoTube AI backend requirements (all free / open-source) ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
PyYAML==6.0.2
tzdata==2025.1            # Windows has no system tz database; required for scheduling
httpx==0.28.1
# Optional: only imported when visuals.ai_image_backend=huggingface.
#
# Needed because Hugging Face retired api-inference.huggingface.co (it no
# longer resolves) in favour of Inference Providers, which dispatch a model to
# a third party - nscale, fal-ai, replicate - each with its own request and
# response shape. This library does that routing; reimplementing it by hand
# would be a per-provider maintenance job.
huggingface_hub==1.30.0
requests==2.32.3
Pillow==11.1.0
numpy==2.2.1
typer==0.15.1
rich==13.9.4
python-dotenv==1.0.1
# --- TTS (free, no key) ---
# 7.0.0 is NOT usable: it predates the `boundary` argument, so the word-timing
# call raises TypeError and the whole provider falls through to gTTS - which
# has no word boundaries and no rate control, silently costing frame-accurate
# captions AND the duration re-fit. 7.0.0 was also the version that started
# returning HTTP 403. This pin was stale for months; the dev laptop only
# worked because it had been upgraded by hand.
edge-tts==7.2.8
gTTS==2.5.4
# --- YouTube (official APIs) ---
google-api-python-client==2.159.0
google-auth==2.37.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
# --- tests ---
pytest==8.3.4
pytest-asyncio==0.25.2
